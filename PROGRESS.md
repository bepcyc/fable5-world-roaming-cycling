# PROGRESS — лог сессии (оценка 0–100 = доля одобренного плана сессии)

- 2026-07-06 **Surface-debug overlay ПОСТРОЕН + 4 бага hueta уничтожены (не закоммичено, вердикт владельца pending).** Сделан отладочный режим `?surfdbg=1`/Shift+W: плоская заливка поверхностей по КЛАССУ + полигональный wireframe (`src/render/DebugSurface.ts` + гейтинг в TerrainTiles/WaterMaterial/TerrainMaterial, легенда DebugOverlay). Лечил дробный `surfIdF` (round+clamp, anomaly=cyan). Уничтожены 4 бага с оригиналов владельца (`shots/wip/hueta/`): **#1 микродороги** (тонкие дороги в ГРЯЗИ — не «короткие»! роутер тянул грунтовки в moisture/ил-флэты → mud-маска `gmud` moisture+pondK +34 штраф в A*, `src/ride/RoadNetwork.ts`); **#2 палки** (гигантские дуги = микродороги по скалам, тот же корень + scatter-гейт брёвен/веток по обоим концам, `src/gpu/passes/Scatter.ts`); **#3 вода-гель** («сначала ямка потом вода»: containment-кламп+DIG в `buildWaterY` + `reconcileWaterAfterCarve` пост-carve, `src/world/Heightfield.ts`); **#4 короткие участки/повороты** (DP-simplify+`enforceTurnSpacing`, асфальт прямые до 322м, зазоры 15–27м; фикс ложного lake-CUT рубившего сеть до 18км). Финал — ЕДИНСТВЕННЫЙ разрешённый заход Fable 5 med (владелец: «последний вызов fable»); приёмка моими руками через overlay. probe-roads ALL PASS 32.6км, typecheck чист. ОСТАЛОСЬ (минор, без Fable): 4–5 точек зазор поворота <20м у бродов (chordSafe запрещает спрямление через воду → мосты/реройтинг backlog), texel-«пила» в руслах. УРОКИ: отлаживать ЧЕРЕЗ overlay а не натуральные кадры; не переопределять баги владельца; пиксели>метрика. Handoff: `.claude/handoffs/2026-07-06-surface-overlay-and-hueta-bug-eradication.md`.
- 2026-07-04 **Сессия 13 — хват-молоток ПОСТРОЕН (класс верный, впервые), полировка кисти не закончена; стоп по команде владельца.** Перед правками ПЕРЕЧИТАН референс глазами (кроп кисти ref-road-aero) — это и дало класс за одну сессию: горб торчит НАД кулаком, ладонь на внешней грани, пальцы через верх вниз по внутренней, большой замыкает. Сделано: (1) параметр `roll` в PoseParams — крен ладонной нормали вокруг оси капюшона к inboard (default 1.05 рад; 0 = старый блин); (2) сгиб θ2/θ3 ЖЁСТКО +bendAxis БЕЗ autoDir (axis×f=palmNormal ⇒ всегда к ладони) — autoDir заворачивал часть пальцев НАРУЖУ вокруг трубы, владелец увидел «обхватываешь изнутри», крыло-растопырка; (3) большой: thumbDir 0.8·fwd+0.5·palm+0.15·down (было вниз-назад: Thumb3 33мм/95мм позади капюшона), P5 оппозиция 40мм PASS; (4) горб возвращён в hoodParts (капсула r=0.0135) + в grip-солид + hornC/hornRad реальные в userData; (5) seatAlong 0.012 (перепонка к горбу); (6) cup maxRad 0.6→1.0 → мизинец дожат (Pinky3 21→6мм); (7) **GRIP_PRESETS {hammer, straight}** — «прямой хват» roll=0.95 сохранён переключаемым по просьбе владельца (dbg.riderGrip.applyPreset). Числа v30: костяшки −1мм, симметрия L=R точная, Ring3 10мм остаток; P1 12.5мм = прежний артефакт метрики (seg2 на тонкой трубе). В бот: свипы roll ×8, seatAlong ×8, v30 ×4, sbs ×3 — все с ОЖИДАЛ/ВИЖУ. ОТКРЫТО: кулак «пластинчатый» (щели ладони, указательный шипом — подозрение на defan при roll>0.5), Ring3, гравийник сидит на капюшонах вместо топсов, MTB-когти, метрика P1→seg3/4. Не закоммичено. Handoff: `.claude/handoffs/2026-07-04-session-13-hammer-grip.md`.

- 2026-07-04 **Сессия 12 — НАЙДЕН и вылечен реальный баг «рука проходит СКВОЗЬ руль» (v24), вердикт владельца pending.** Начал с косметики (рожок капюшона слит геометрией, proud +13→−3мм) — владелец ОСТАНОВИЛ: «прогресса нет; рука не хватает руль, проходит сквозь него». Он прав, и это ключевой урок: **probe-grip ALL PASS был ЛОЖНЫМ сигналом** — мерил капсулу капюшона, а видимая аэро-труба шла СКВОЗЬ кисть (пересечение мешей, не хват). Диагноз числами `tools/probe-bar.ts` (слои y/z в root): **аэро-сдвиг капюшона `(0,0.004,−0.026)` увёл GRIP-ЦЕЛЬ на 26 мм ВПЕРЁД от реального руля** → кисть садилась впереди трубы, труба под ладонью (между запястьем и костяшками), пальцы торчали в воздух ЗА рулём (кончики 80 мм от трубы). Плюс видимой трубы (аэро-крыло + дроп) НЕ было в grip-солиде (ловушка скилла: моделируй ВЕСЬ солид, иначе солвер не смыкает хват). Фикс (гнал по ПИКСЕЛЯМ, зумы контакта, не по метрике): (1) убрал аэро-сдвиг `hp=hp0` → костяшки сели НА трубу (gapToDrop 9-18→3-5 мм); (2) добавил реальную трубу `dropBarPoints[3..7]` (round `barR`) в grip-солид `RiderBody` + spec; (3) `curl` floor 0.15→0.22, maxRad фаланг 1.15→1.9 → пальцы обходят вниз-ПОД трубу; (4) убрал рожок (после сдвига назад он клиппил пальцы); (5) v26: grip-цель (hoodR-оффсет −0.016→−0.002) придвинута на САМУ трубу → форвардные пальцы перестали тянуться к капюшону/рычагу впереди, оба ИНДЕКСА сомкнулись (gapToDrop 51→1-5мм), L-средний перелёт 51→30мм. Итог: **правая кисть уверенно ХВАТАЕТ руль; левая хватает, L-средний слегка перелетает** (остаток bind-асимметрии L/R). В бот: зумы хвата v19→v24 + before/after v18(сквозь)→v24(хват) + sbs ×3, все ОЖИДАЛ/ВИЖУ. ОТКРЫТО: (а) L-перелёт среднего/индекса — асимметрия рига; (б) probe-grip P1 12-14мм это КОСТЯШКА (seg2) на тонкой трубе Ø24 (тоньше капсулы 0.018) + перекрытие капсул, НЕ клип (кончики seg3/4 ±4мм) → метрику ПЕРЕСЧИТАТЬ под новую геометрию (мерить seg3/4, костяшка = P4); (в) тест истончения капюшона 0.018→0.014 (v25) дал дыры в кисти — откачен. Новые тулзы: `probe-bar`, `ck-norider` (руль без райдера), probe-horn, probe-sweep, ck-pose-sweep. Не закоммичено (owner WIP `tools/launch-gpu.ts` не трогал). **УРОК владельца: пиксели > метрика; называть вещи своими именами (сквозь ≠ хват); probe ALL PASS ≠ визуально верно.** ГЛАВНОЕ (владелец поймал в конце): достигнутая симметрия — хват НЕ ТОГО КЛАССА. Нужен хват КАК МОЛОТОК (кисть замыкается вокруг капюшона, большой ПРОТИВ пальцев, капюшон+горб В КУЛАКЕ, перепонка на горбе), а я делал КАК ВИСЯТ НА ПЕРЕКЛАДИНЕ (ладонь плоско на трубе, пальцы крючком, большой не противопоставлен) = грипса MTB — свести нельзя, разные классы. Следующая сессия: вернуть горб, замкнуть кулак вокруг капюшона (большой-оппозиция), симметрию даст `mirrorRightHandToLeft` даром. Handoff: `.claude/handoffs/2026-07-04-session-12-road-grip-wrong-type.md`.

- 2026-07-04 **Сессия 11 — руки ШОССЕЙНИКА обхватили капюшоны (v15), вердикт владельца pending.** Найден числами корень всех «блинов на топах» v3–v14: рука Adventurer на ~15 см не доставала до капюшонов (solveArm молча клампил, запястья на t=−172…−176 мм; t-кламп капсулы маскировал недолёт в метрике) + все 4 пястные кости колоцированы у запястья (фантомные «96 мм» старой P4). Позинг переписан на «по построению»: reach-композиция (таз на нос седла → шарнир торса с гардом ≤0.35 rad → удлинение рук с контр-масштабом кисти), самоцентровка ладони по 3 осям (вдоль/латераль/высота к +2 мм), ролл запястья до поцелуя ring-костяшки, де-фан фаланг на seg2 (веер seg1 = разнос костяшек, не трогать), купол пястных пер-палец, контактный солвер curlToContact (первое касание фаланги min(середина,конец), двунаправленный, autoDir) к солиду капюшон∪рычаг, большой палец aim внутрь+контакт. Метрика probe-grip честно доработана (рычаг в солиде, дистанция до отрезка, P4=MCP-ряд, PEN 6 мм с обоснованием сагитты, P5 большой-изнутри): **ALL PASS** (пенетрация L 2.3 / R 1.5 мм; было L раскрыта / R блин). Регрессии: probe-menu-ride ALL PASS, tsc чист. В бот: sbs ×3 (road/gravel/mtb) + close + кроп руки vs кроп референса, все с ОЖИДАЛ/ВИЖУ; sbs-guard pending пуст. Открыто: gravel tops / MTB grips (per-mode спеки, паттерн готов), рожок торчит выше кисти, качание рук с кокпитом, митенки. Handoff: `.claude/handoffs/2026-07-04-session-11-road-hands-grip.md`. Не коммичено (Adventurer-линия ждёт вердикта).

- 2026-07-03 **100/100 — M1.4 «BLE-сенсоры» ЗАКРЫТ (сессия 4, без реального железа по директиве).** Сначала исследование (условие владельца): docs/notes/ble-ftms-research.md — 106 агентов deep-research + 3 целевых раунда, 117 фактов, 25 проверены адверсариально, 1 опровергнутый факт перепроверен по PDF спеки FTMS; все ловушки зафиксированы (инвертированный бит 0 в Indoor Bike Data, каденс ×2 на проводе, CPS 1/2048 с vs CSC 1/1024 с, Request Control перед SIM, Web Bluetooth на Linux за флагом, headless без BT-стека). Итоги исследования отправлены в бот. Сдано: src/ride/ble/ — Parsers.ts (чистые защитные парсеры HR/CPS/CSC/FTMS: не бросают исключений на мусоре, модульная арифметика rollover), Transport.ts (адаптер: WebBluetoothTransport + FakeTransport для проб — граница тестируемости), BleSensorSource.ts (4 слота trainer/power/csc/hr, приоритеты каналов, staleness 3 с → null, SIM-градиент: Request Control → Set Indoor Bike Simulation с живым уклоном и честным Crr поверхности, rate-limit, re-request при потере контроля, read-only fallback), ConnectUi.ts (панель BLUETOOTH SENSORS — настоящие кнопки, user gesture обязателен; подсказка про chrome://flags на Linux). Шов расширен: setSimState в SensorSource (опционально), honesty-гейт в BikeRig — BLE без живых ватт держит велосипеды запертыми. Приёмка tools/probe-ble.ts ALL PASS: 21 pure + 16 live, в т.ч. P6 обрыв (ватты сразу null, без краша, честный накат в горку до остановки 2.8→0 км/ч — вниз по склону накат честно держит равновесную скорость, поэтому детерминированный uphill-runway; RECONNECT в панели; повторное подключение возвращает ватты), P7 бейджи (BLE без бейджа, demo с бейджем), SIM-запись наблюдаема (0x00 первым, wire grade == rig grade до 1e-4). Регрессии probe-physics/roads/surface/ridehud ALL PASS, typecheck чистый. Кадры probe-ble-{riding,dropout}.png отправлены владельцу с русскими пояснениями. Вход следующей сессии: **M1.5 кокпит + дашборд v2 + P5 предупреждения** (руль/руки/велокомпьютер на логической позе, TRAA-митигация, surfaceAt lookahead).
- 2026-07-03 **100/100 — M1.3 «режимы движения и физика» ЗАКРЫТ (сессия 3).** Мир теперь проезжаем: hike ⇄ road/gravel/mtb (клавиша M), физика power→speed на fixed-timestep. Сдано: чистый решатель `src/ride/BikeSolver.ts` (сопротивление качению по матрице + уклон + аэро, реальные массы/CdA, governor на maxSpeed как поведение райдера, blocked = вынужденный спешивающий стоп, увязание на stallRisk-поверхностях), fixed-timestep аккумулятор в Engine (`?dt=`, интерполяция позы по fixedAlpha — плавно при любом шаге), граф маршрутов `RouteGraph` (узлы на пересечениях/якорях: 38 узлов / 46 рёбер), `BikeRig` (посадка ≤30 м от дороги, движение по рёбрам, тупик = честный разворот), **выбор поворота на развилке** — Zwift-стиль «TURN AHEAD · N м» с карточками-стрелками по классам дорог, выбор ←/→ (директива владельца «не по рельсам»). Дашборд перерисован: стеклянные карточки SPEED/POWER/GRADE/CADENCE/HR + чип режима + бейджи DEMO/DEV + баннеры состояний; `?ridedev=1` = клавиатурный велосипед (W крутить, +/- ватты, Shift рывок, S/Space тормоз). Hike пересужен под Pillar B: **гравитация 22 → 9.81**, прыжок ~0.55 м, sprint-FOV убран. .fit-рекордер получает реальные ватты/скорость. Приёмка `tools/probe-physics.ts` ALL PASS: P1 стоп в грязи / P2 максималка ±2% (факт 0.00%, разброс по dt 0.000%) / P3 брод / P4 блок по уклону — на 3 шагах dt (33.3/8.3/2.1 мс) + живой движок: дашборд==солвер ±1% (0.08–0.11%) на двух dt, chooser появляется перед реальной развилкой, велосипеды заблокированы без источника мощности. Регрессии probe-roads/probe-surface/probe-ridehud ALL PASS, typecheck чистый. Кадры: shots/wip/probe-physics-{ride,junction}.png. Вход следующей сессии: **M1.4 BLE-сенсоры** (шов готов: powerW в RideSample, гейт по источнику).

- 2026-07-03 **100/100 — M1.2 «дороги и тропы» ЗАКРЫТ (сессия 2).** Сеть 30.8 км / все 5 классов, врезана в рельеф (конформность ≤13 см при допуске 15), заштампована в карту поверхностей, растительность с полотна убрана (GPU-аудит: 0 нарушений). Приёмка probe-roads ALL PASS + регрессии probe-surface/probe-ridehud ALL PASS + typecheck. Директивы владельца этой сессии все закрыты: реализм-калибровка по веб-исследованию (Stelvio/IMBA/ROADEX → `docs/notes/climb-grade-facts.md`), многоядерность (`?threads`/F4, worker-pool, дефолт = все ядра), меню графики (F5: low/high/ultra, ориентир high = 30–60 fps на RX 6800 XT), серпантин с автопоиском вершины (ridge-dirt, подъём/спуск с широкими галсами), гравий перекрашен в зернистый (фидбек «не похож»), спавн для проверки `?road=класс`, черновой .fit-экспорт (Ctrl+E, без тестирования — по ТЗ), развилки-выбор = M1.3 (сеть уже граф). Вода «над землёй» на скринах = унаследованные артефакты LAAS (реестр §6), не регрессия. Пять вылеченных походовых багов задокументированы в STATUS (ромбы полотна, двойная врезка, исчезающий синглтрек, старт в русле, фантомная глубина под висячим озером). Вход следующей сессии: **M1.3 физика движения** (fixed-timestep, power→speed по матрице, руление по маршруту + выбор поворота на развилке).
- 2026-07-03 *(промежуточная запись сессии 2, история ниже)* **80/100 — M1.2 дороги: ядро сдано, probe-roads ALL PASS.** Сеть 31.8 км, все 5 классов: `src/ride/RoadNetwork.ts` (grade-limited A* по эродированному рельефу — серпантины рождаются из лимита уклона, реальные инженерные параметры: асфальт 5.8 м/≤12% как альпийские перевалы, singletrack 1.1 м/≤22%; вираж из кривизны, дренажный купол, броды = профиль по дну), `src/gpu/passes/RoadField.ts` (carve с насыпями 1:1.6 после composeEroded, запечённое поле 2048² — ЕДИНЫЙ сэмплер для материала/травы/скаттера/аудита, штамп SurfaceId 10–14 в карту M1.1, вода в приоритете на бродах). Материал: палитры по классам, колеи, травяная полоса на грунтовке, изношенная обочина, гейт микрорельефа (lockstep vertex+fragment). Исключение растительности в обоих местах (Scatter ×4 ядра + GroundRing ×3). Приёмка `tools/probe-roads.ts`: конформность ≤15 см (факт ≤9 см вне перекрёстков), 0 подводных вне бродов, штамп ≥95%, уклоны в лимитах классов, GPU-аудит 0 деревьев/камней на полотне (band-контроль поля жив). Три пойманных и вылеченных бага: ромбы на полотне (наложенные коридоры маршрутов → дедуп через netMask-штраф A*), ступени ±2 м (самодублирование коридора внутри маршрута → маркировка по плечам), заросший singletrack (unsigned dist в запечённом поле терял узкую тропу → signed lateral). Кадры: shots/wip/probe-roads-{asphalt,gravel,dirt,single}.png — асфальт/грэвел/грунтовка хороши, тропа читается. Осталось: директива многоядерности + пункт меню, регрессии probe-surface/probe-ridehud, STATUS, коммит.

- 2026-07-02 **100/100 — M1.1 «слой поверхностей» ЗАКРЫТ (сессия 1, уже на pop-os).** Переезд на целевую машину прошёл гладко: node 23 установлен, `tools/launch-gpu.ts` без изменений даёт headless-адаптер **amd/rdna-2** (RX 6800 XT), boot 35–42 s. Сдано: `src/ride/SurfaceMatrix.ts` (единый файл истины поверхность×режим — 15 классов, Crr/скорости/grip/звук/погода/stall + лимиты уклона P4 + пороги воды/грязи), GPU-классификатор `src/gpu/passes/SurfaceClassify.ts` (повторяет сплат-веса материала 1:1 → нулевой дрейф от картинки; вода из waterY−ground; болотная грязь), CPU-зеркало 4096² в Heightfield + `surfaceAtCpu` (majority 3×3 против фликера на границах — главный риск M1.1 закрыт), `slopeAtCpu`, `waterDepthAtCpu`, groundProbe расширен до `{ground, water, surfaceId, slope}`. Приёмка: `tools/probe-surface.ts` — 4 транссекта по известным ориентирам + мировая сетка 96², **30/30 PASS** (совпадение с визуальной правдой 100% на линиях, скрины сверены глазами; вода⟺глубина 99+%), регрессия probe-ridehud 7/7 PASS, typecheck чистый. Первый прогон был 28/30 — оба фейла оказались ошибками ОЖИДАНИЙ (на «луговой» линии реально есть скальные гряды и ручей — видно на shots/wip/probe-surface-meadow-forest.png; отвесная стена кара 6.2 rise/run — законный рельеф). STATUS дополнен, вход следующей сессии: **M1.2 дороги**.

- 2026-07-02 **ФИНАЛ: Q1–Q10 отвечены владельцем — гейт реализации ОТКРЫТ.** Ответы вписаны в `docs/OPEN-QUESTIONS.md` (теперь decision record): pop-os = целевая; сид-детерминизм дорог; headless-пробы; RANDO; мощность — драйвер физики; **FTMS SIM-сопротивление — в MVP**; демо всем с бейджем; **BLE пишем сейчас без живого теста** (донгл и тест всех сенсоров — отдельной сессией позже, вынесено из M1.4); естественная физика по рекомендации; дефолты райдера приняты. ROADMAP M1.4/M3.2 скорректированы, handoff и STATUS обновлены. Следующая сессия: **M1.1 слой поверхностей** с ходу. Стоп по команде владельца.
- 2026-07-02 **100/100 + бонус: pop-os обследован.** SSH-загвоздка решена (ключ живёт в gcr-ssh-agent `/run/user/1000/gcr/ssh`, а не в gpg-agent-сокете из env). Машина: **Ryzen 9 3950X / 128 GB / RX 6800 XT (RADV NAVI21)**, Pop!_OS 24.04, Chrome 149, X11, 3440×1440 — очевидный кандидат в целевую игровую (Q1 обновлён); node/npm там нет (`just run-rxgpu` теперь с гардом и подсказкой nvm), BT-адаптера нет и там (Q8 обновлён — донгл нужен в любом случае). Заметка: `docs/notes/pop-os-target-machine.md`; handoff дополнен.
- 2026-07-02 **100/100 — сессия 0 закрыта.** Верификатор (свежий контекст) проверил ~120 клеймов доков: 2 неверные ссылки-указателя + 2 неточные строки — исправлены; перф-таблица сошлась с сырым логом байт-в-байт; баг protухших sun-униформ подтверждён реальным. Justfile дополнен `run-rxgpu` (Pop!_OS/AMD RX, best-effort — ssh-ключ на pop-os не авторизован, сервер просит пароль; проверить после `ssh-copy-id`). WebGPU-уточнение владельца внесено: интерактивному Chrome флаги не нужны, рецепт — только для headless-тулинга. Полный handoff для новой сессии: `.claude/handoffs/2026-07-02-210230-rando-session-0-planning.md` (валидатор: READY) + указатель в STATUS. Ветка `rando`, 5 коммитов. Реализация НЕ начата дальше одобренного слайса — по инструкции владельца ждёт ответов на `docs/OPEN-QUESTIONS.md` Q1–Q10.

- 2026-07-02 **78/100** — Ветка `rando`, 4 коммита. Замечание ревью «верификация HUD невоспроизводима» закрыто: `tools/launch-gpu.ts` (лаунчер с assert'ом реального GPU — SwiftShader отвергается), `tools/probe-ridehud.ts` (P7 + весь контракт дашборда, зелёный прогон 7/7 PASS на amd/gcn-5, exit code), `tools/perf-baseline.ts` (воспроизводимый avg+1%low сьют за PERF-BASELINE.md). Пункты «PERF-BASELINE отсутствует» и «STATUS не обновлён» были устаревшим снапшотом — оба файла закоммичены раньше. Осталось: отчёт свежего верификатора по докам → сверка → финал. — Все четыре плановых документа готовы: `docs/PROJECT_RIDE_v1.md` (бриф, кодовое имя RANDO, пиллары: эстетика без тяп-ляп, естественная физика, честные данные, одна матрица, probes P1–P7), `docs/ROADMAP.md` (M1.1→M3.3, BLE в MVP, high-speed pass последним и на целевой машине), `docs/OPEN-QUESTIONS.md` (Q1–Q10 с рекомендациями — ответь построчно), `docs/PERF-BASELINE.md` (native 3440×1440: bm1 4.7 / bm3 3.3 / bm7 4.5 avg fps, 1%low 2.9–4.0, движение 3.9; boot 46–63 s; fps-приоритет понижен по директиве — 1080p/low-пресет не мерил). Плюс `docs/notes/` ×3 (NixOS-рецепт WebGPU, один-boot-много-проб, flythrough≠велоскорость) и большая FORK-секция в STATUS.md (вход для следующей сессии: ответы на вопросы → починка launch.ts → M1.1). Осталось: свежий верификатор по всем докам + сверка + финал. — Ride-HUD верифицирован на живом движке headless-прогоном (8/9 чеков; девятый — баг регекспа самой пробы, данные в логе верные: SPEED 228.3 km/h в полёте — реальная скорость flythrough-камеры, HR 114→147 растёт с усилием, каденс 83–86, DEMO-бейдж, B-переключение, скрыт без ?ride). Скриншот-контроль: карточки читаемы, в стиле движка. Мир грузится на Vega 7 за ~63 s (M1-бюджет был ≤15 s); первый перф-факт: ~8 fps @1600×900 в движении. `docs/ARCHITECTURE-ASSESSMENT.md` написан (точки подключения, global-vs-tileable + вердикт по Phase-2 стримингу, риски на велоскорости, реестр унаследованных багов). Justfile: `just run` (универсальный dev-сервер) + `just run-nixos` (Chrome с Vulkan-флагами, отдельный профиль). Директива качества принята: естественная физика (гравитация/скорости/FOV), выверенная эстетичная графика — уйдёт в бриф как hard rules; унаследованная walk-гравитация 22 m/s² помечена на пересмотр. Запущен полный FPS-бейзлайн (закладки + движение, native 3440×1440 + 1080p, high + low пресет) — идёт в фоне ~15 мин.

- 2026-07-02 **22/100** — Среда поднята: npm-зависимости, vite на :5173. NixOS-затык с WebGPU решён: playwright-chromium на NixOS не стартует вовсе, headless-рецепт = системный Chrome + `--enable-unsafe-webgpu --enable-features=Vulkan --use-angle=vulkan` → настоящий адаптер AMD gcn-5 (RADV Vega), не SwiftShader; repo-launcher (tools/launch.ts) этот рецепт не умеет — follow-up на будущую сессию. Ride-HUD слайс реализован: `src/ride/Sensors.ts` (сенсорная абстракция + DemoSensorSource), `src/ride/RideHud.ts` (Zwift-стиль: SPEED/CADENCE/HR, бейдж DEMO, клавиша B, `?ride=1|demo`), подключение в main.ts после камеры; typecheck чистый. Запущена headless-верификация HUD на живом движке.
